Coding Standards
================

- Format code with ``yapf``: ``yapf -i <path>``
- Run type checks with ``mypy``: ``mypy -p dcdft``
- Run linting with ``flake8``: ``flake8 dcdft``
- Write NumPy-style docstrings.
